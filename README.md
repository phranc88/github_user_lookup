# github_user_lookup

Command line tool that looks up GitHub users and repos.

## Usage

Look up a user:

```sh
go run . user octocat
```

Look up a repo:

```sh
go run . repo octocat Hello-World
```
